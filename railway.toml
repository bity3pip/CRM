[build]
dockerfile = "Dockerfile"

[deploy]
startCommand = "sh -c 'python manage.py migrate && python manage.py seed && daphne -b 0.0.0.0 -p $PORT config.asgi:application'"
restartPolicyType = "on_failure"